# Optional dependencies for SGLang-backed MoE acceleration.
# Install after requirements.txt with:
#   python -m pip install --no-deps -r requirements-sglang.txt
#
# `--no-deps` keeps the tested PyTorch/CUDA stack from requirements.txt intact.
sglang==0.5.13.post1
apache-tvm-ffi==0.1.9
tilelang==0.1.8
sglang-kernel==0.4.4
triton>=3.6.0
